function Navbar(){
    return(
        <nav>
            <h2>Creative Writing Tracker</h2>

            <div>
                <a href="#">Projects</a>
                <a href="#">Goals</a>
                <button>+ New Project</button>
            </div>
        </nav>
    )
}

export default Navbar;